# Cheatsheet helper (example)
# Add your most common commands, tips, or reminders here for quick access.

cheat() {
  echo "--- Common SRE/DevOps Commands ---"
  echo "kubectl get pods -A"
  echo "stern <pattern> --tail=100"
  echo "promtool check config prometheus.yml"
  echo "docker ps -a"
  echo "terraform plan"
  echo "aws s3 ls"
  echo "k6 run load.js"
  echo "# Add more as needed..."
}
