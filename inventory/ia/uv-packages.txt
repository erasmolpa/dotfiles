# Python packages installed with `uv pip install` when missing.
# Scope: active venv if present; otherwise system site-packages via `--system`.
huggingface_hub
langchain
langgraph
llama-index
openai
pydantic
rich
tiktoken
typer
